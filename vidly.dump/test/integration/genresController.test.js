const request = require("supertest");
const app = require("../../src/app");

describe("/api/genres", () => {
    describe("GET", () => {
        it("Should return all genres", async () => {
        });
    });
});