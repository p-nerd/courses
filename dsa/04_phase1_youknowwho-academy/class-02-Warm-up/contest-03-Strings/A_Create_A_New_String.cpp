#include <bits/stdc++.h>
using namespace std;

#define _end '\n'
#define _vec vector
#define _str string
#define _len(a) a.size();

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    _str s1, s2;
    cin >> s1 >> s2;

    cout << s1.length() << " " << s2.length() << _end;
    cout << s1 << " " << s2 << _end;

    return 0;
}
